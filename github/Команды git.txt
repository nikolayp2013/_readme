git init
Эта команда создаёт в текущем каталоге новый подкаталог с именем .git содержащий все необходимые файлы репозитория — основу Git-репозитория.

git add README.md
Добавить под версионный контроль существующие файлы

git commit
Фиксация изменений
Появится редакто vim. Нужно написать комментарий к изменениям. Для этого в английской раскладке нажимаем a и в "вводим комметарий к коммиту". Затем нажимаем клавише esc, 
затем нажимаем : (двоеточие), затем w (write) и q (quit), получилась строка :wq  
Нажимаем Enter

git commit -m "initial project version"

git clone git://github.com/nikolayp2013/my-project.git
Клонирование существующего репозитория

git status
Определение состояния файлов

git add README
Добавить новые файлы для отслеживания

git diff
Чтобы увидеть, что же вы изменили, но пока не проиндексировали

git rm grit.gemspec
Удаление файлов

git mv file_from file_to
Перемещение файлов

git push origin master
 !!! Передача изменений на удаленный сервер !!!

git remote set-url origin https://github.com/nikolayp2013/my-project.git
Если возикает ошибка при выполении команды push

git fetch
Команда git fetch связывается с удалённым репозиторием и забирает из него все изменения, которых у вас пока нет и сохраняет их локально.

git pull
Команда git pull работает как комбинация команд git fetch и git merge, т.е. Git вначале забирает изменения из указанного удалённого репозитория, а затем пытается слить их с текущей веткой.